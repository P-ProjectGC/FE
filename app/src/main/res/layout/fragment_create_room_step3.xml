<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/layout_step3_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="16dp"
    android:paddingBottom="24dp">

    <!-- 여행방 정보 카드 -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_room_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="16dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="@android:color/white">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- 🔹 여행방 이름 라벨 + 아이콘 -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:id="@+id/iv_room_name_icon"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/icon_room_name" />

                <TextView
                    android:id="@+id/tv_room_name_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="6dp"
                    android:text="여행방 이름 *"
                    android:textSize="14sp"
                    android:textColor="#0A0A0A" />
            </LinearLayout>

            <!-- 방 이름 입력 박스 -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/card_room_name_input"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="4dp"
                app:cardCornerRadius="14dp"
                app:cardBackgroundColor="#EDF8FD"
                app:strokeColor="#A8DEF5"
                app:strokeWidth="1dp">

                <EditText
                    android:id="@+id/et_room_name"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@android:color/transparent"
                    android:hint="예: 제주도 가족 여행"
                    android:paddingHorizontal="12dp"
                    android:singleLine="true"
                    android:textSize="14sp" />
            </com.google.android.material.card.MaterialCardView>

            <TextView
                android:id="@+id/tv_room_name_hint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="여행 목적지나 테마를 입력해보세요"
                android:textSize="12sp"
                android:textColor="#777777"
                android:layout_marginBottom="16dp" />

            <!-- 🔹 메모 라벨 + 아이콘 -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:id="@+id/iv_room_memo_icon"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/icon_room_memo_rm" />

                <TextView
                    android:id="@+id/tv_memo_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="6dp"
                    android:text="메모 (선택사항)"
                    android:textSize="14sp"
                    android:textColor="#0A0A0A" />
            </LinearLayout>

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/card_room_memo_input"
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:layout_marginTop="8dp"
                app:cardCornerRadius="14dp"
                app:cardBackgroundColor="#EDF8FD"
                app:strokeColor="#A8DEF5"
                app:strokeWidth="1dp">

                <EditText
                    android:id="@+id/et_room_memo"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@android:color/transparent"
                    android:gravity="top|start"
                    android:hint="여행에 대한 간단한 메모나 계획을 작성해보세요.\n예: 맛집 투어, 해변 휴양, 등산 등"
                    android:padding="12dp"
                    android:textSize="13sp"
                    android:inputType="textMultiLine"
                    android:maxLines="5" />
            </com.google.android.material.card.MaterialCardView>

            <!-- 메모 글자 수 -->
            <TextView
                android:id="@+id/tv_memo_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="end"
                android:layout_marginTop="4dp"
                android:text="0/500자"
                android:textSize="11sp"
                android:textColor="#999999" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- 하단 '완료' 버튼 -->
    <Button
        android:id="@+id/btn_complete_step3"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="완료"
        android:enabled="false"
        android:textColor="#B3FFFFFF"
        android:background="@drawable/bg_btn_next_disabled"
        android:layout_marginTop="16dp"
        android:backgroundTint="@null" />

</LinearLayout>
