<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 배경색 (흰색) -->
    <solid android:color="#FFFFFF"/>

    <!-- 둥근 모서리 -->
    <corners android:radius="6dp"/>

    <!-- 외곽선 -->
    <stroke
        android:width="2dp"
        android:color="#80000000"/>  <!-- 50% 투명 검정 -->

    <!-- 패딩 -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="6dp"
        android:bottom="6dp"/>
</shape>